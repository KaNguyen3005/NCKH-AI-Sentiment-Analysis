# ==========================================
# 1. Core Data Manipulation & EDA
# ==========================================
pandas
numpy

# ==========================================
# 2. Machine Learning & Statistics
# ==========================================
scikit-learn      # Naïve Bayes, SVM, TF-IDF, Metrics (Accuracy, F1)
statsmodels       # McNemar test (Task Đánh giá Tuần 3)

# ==========================================
# 3. NLP & Deep Learning (PyTorch Ecosystem)
# ==========================================
torch             # Framework chính cho BiLSTM, GRU, PhoBERT
transformers      # Load vinai/phobert-base và mBERT từ HuggingFace
safetensors       # Lưu trọng số PLM ở định dạng an toàn
underthesea       # Tiền xử lý, tách từ tiếng Việt chuẩn (Task Tuần 1)
fasttext-wheel    # Xây dựng embedding layer cho BiLSTM/GRU (dùng bản wheel để tránh lỗi build C++)

# ==========================================
# 4. Visualization (EDA & Evaluation)
# ==========================================
matplotlib        # Vẽ Learning curve
seaborn           # Vẽ heatmap cho Confusion matrix
wordcloud         # Hiển thị top từ khóa mỗi nhãn (Câu hỏi 4 - EDA)

# ==========================================
# 5. LLM API (Đối chứng Zero-shot/Few-shot)
# ==========================================
openai            # API gọi mô hình GPT 
google-genai       # SDK Gemini chính thức hiện hành

# ==========================================
# 6. Tuning, App & Utilities
# ==========================================
streamlit         # Xây dựng Web App Demo (Task Tuần 4)
optuna            # Hyperparameter tuning (lr, batch, epoch cho PhoBERT)
tqdm              # Hiển thị thanh tiến trình (rất cần khi code vòng lặp train/test hoặc LLM)
notebook          # Môi trường chạy các file .ipynb (01_EDA.ipynb -> 07_llm_baseline.ipynb)
PyYAML            # Đọc cấu hình config.yaml
python-dotenv       # Đọc API key từ file .env cục bộ
